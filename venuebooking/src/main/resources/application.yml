server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/venue_booking?useSSL=false&serverTimezone=UTC
    username: root
    password: Lingyi@2025
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
